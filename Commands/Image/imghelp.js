const execute = async (client,msg) => {
    msg.reply('- Send a pic and quote(mention) $command \n- For better results use a square pic(more like a box).\n- Due to an issue with webp on nodejs stickers are not supported yet.');
}

module.exports = {execute};